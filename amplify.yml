version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install # or yarn install
    build:
      commands:
        - npm run build # or yarn build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
