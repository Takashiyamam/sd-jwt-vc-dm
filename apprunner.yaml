version: 1.0
runtime: nodejs18
build:
  commands:
    pre-Build:
      - pwd
      - npm install -g pnpm
      - pnpm install
      - cd examples/nextjs-playground/
      - pnpm install
    build:
      - pwd
      - pnpm build
      - cd examples/nextjs-playground/
      - pnpm build
run:
  command: pnpm start
