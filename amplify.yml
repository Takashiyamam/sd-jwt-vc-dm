version: 1
applications:
  - appRoot: "examples/nextjs-playground"
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../../
            - pwd
            - npm install
            - npm build
            - cd examples/nextjs-playground/
            - npm install # install from root            
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
